import React from "react";
import "./styles/spinner.css"; // Ensure the CSS file is linked

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;